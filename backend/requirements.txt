# ── Web Framework ────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ── LLM / Groq API ──────────────────────────────────────────────────
groq==0.13.1

# ── LangGraph / LangChain ───────────────────────────────────────────
langgraph==0.2.60
langchain-core==0.3.28

# ── Vector Database + FastEmbed (replaces sentence-transformers+torch) ─
# [fastembed] pulls in the ONNX runtime + bge-small model (~40 MB total)
# Zero GPU required — runs on CPU via ONNX
qdrant-client[fastembed]==1.12.1

# ── HTTP clients (async Groq Whisper upload, retries) ────────────────
httpx==0.28.1

# ── Audio validation (pydub uses ffmpeg binary, NOT torch) ───────────
pydub==0.25.1

# ── Utilities ───────────────────────────────────────────────────────
python-dotenv==1.0.1
supabase==2.11.0
tenacity==9.0.0
pydantic==2.10.4
loguru==0.7.3
aiofiles==24.1.0
numpy==1.26.4        # qdrant-client needs it for vector ops
